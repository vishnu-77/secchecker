# README animation

The source animation is `secchecker-hero-animated.svg`.

GitHub currently does not execute inline SVG animation, so README embedding should use a rendered bitmap animation (`secchecker-hero.gif` or animated WebP) while the SVG remains the editable source for docs/web use.
